|
  $('#user-creation-form').html("#{j render partial: 'new_form', locals: { object: @user}}")
  $('#user-creation-modal').on('shown.bs.modal', () => {
	  $('input#fullname').focus();
  }).modal('show');